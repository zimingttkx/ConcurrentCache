# 第一章：概述与设计理念

## 1.1 项目背景

### 1.1.1 为什么要开发这个项目？

在互联网后端开发中，**缓存**是提升系统性能的关键技术。Redis 作为最流行的内存数据库，被广泛应用于各大互联网公司。

```
没有缓存：
用户请求 ──► 数据库查询 ──► 返回结果
                    │
                    ▼ 慢！数据库压力大！

有缓存：
用户请求 ──► 缓存查找 ──► 命中！返回
                              │
                              ▼ 未命中
                        数据库查询 ──► 写入缓存 ──► 返回
```

但是有时候：
1. **只是想学习 Redis 的工作原理**
2. **项目太小，不需要引入完整的 Redis**
3. **想要一个可以嵌入的轻量级缓存**

于是你决定：**自己写一个简单的缓存服务器**。

---

## 1.2 当前版本状态

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ConcurrentCache 版本状态                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ✅ V1 骨架版本（2026-04-24 之前）                                        │
│  ├── 单 Reactor 网络模型                                                  │
│  ├── RESP 协议解析                                                      │
│  ├── 命令工厂 + 4 个基础命令                                            │
│  └── GlobalStorage 全局存储                                              │
│                                                                             │
│  🔄 V2 基础版本（2026-04-24 开始）                                      │
│  ├── 🔄 Logger：Sink 抽象 + 模块支持 + 热加载（代码完成）                │
│  ├── 🔄 Config：观察者模式 + 热加载接口（代码完成）                      │
│  ├── ✅ Format：统一格式化工具                                          │
│  ├── 📋 Signal：SIGSEGV 堆栈 + SIGPIPE                                │
│  ├── 📋 锁机制：Mutex/SpinLock/RWLock                                  │
│  ├── 📋 内存池：三级分层                                                │
│  ├── 📋 线程池                                                          │
│  ├── 📋 MainSubReactor 多线程网络                                       │
│  ├── 📋 分段锁哈希表                                                    │
│  ├── 📋 过期字典 + LRU                                                  │
│  └── 📋 命令增强                                                        │
│                                                                             │
│  📋 V3 增强版本（计划中）                                               │
│  └── Hash/List/Set/ZSet + RDB/AOF                                       │
│                                                                             │
│  📋 V4 分布式版本（计划中）                                             │
│  └── 哈希槽 + Gossip + 主从复制                                         │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 1.3 V1 骨架版本目标

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         V1 骨架版本                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  目标：开发一个最小可运行的 Redis 风格缓存服务器                          │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  功能目标                                                            │  │
│  │  □ 接受 TCP 连接                                                     │  │
│  │  □ 解析类似 Redis 的命令协议                                          │  │
│  │  □ 支持基本键值操作（GET/SET/DEL/EXISTS）                            │  │
│  │  □ 线程安全存储数据                                                  │  │
│  │  □ 高性能、高并发                                                     │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  质量目标                                                            │  │
│  │  □ 代码结构清晰，模块化设计                                          │  │
│  │  □ 注释详尽，作为学习资料                                           │  │
│  │  □ 易于理解和扩展                                                   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### V1 约束与挑战

| 约束 | 说明 | 应对 |
|------|------|------|
| 单线程 Reactor | 只能使用一个 CPU 核心 | 先实现功能，后续版本升级多线程 |
| 简单存储 | 只用 unordered_map | 先能用就行，后续版本加 TTL/LRU |
| 有限命令 | 只实现 4 个命令 | 先验证架构，后续版本扩展 |
| 无持久化 | 数据在内存，重启丢失 | 后续版本支持 RDB/AOF |

**设计原则**：先解决"能不能跑"的问题，再解决"跑得好不好"的问题。

---

## 1.4 整体架构

### 1.4.1 分层架构（当前状态）

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户层                                         │
│                    redis-cli 或任何 TCP 客户端                               │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 │ TCP 连接 + RESP 协议命令
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           应用层 (Application)                               │
│                                                                             │
│  命令系统 ─── 用户的命令是什么？                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  CommandFactory  │  命令工厂                                          │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐                    │  │
│  │  │ GET命令 │ │ SET命令 │ │ DEL命令 │ │ EXISTS │  ...               │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘                    │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ⚠️ V2 将增加：INCR/DECR/SETNX/SETEX/APPEND/STRLEN                    │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 │ 命令参数数组 ["SET", "name", "zhangsan"]
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           协议层 (Protocol)                                 │
│                                                                             │
│  RESP 协议 ─── 如何理解这些命令字符串？                                     │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  RespParser  │  解析器：将 TCP 字节流转为结构化数据                   │  │
│  │  RespEncoder │  编码器：将响应数据编码为 TCP 字节流                   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 │ RespValue { type: ARRAY, data: [...] }
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           存储层 (Storage)                                   │
│                                                                             │
│  全局存储 ─── 数据存在哪里？如何保证线程安全？                             │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │  GlobalStorage ─── 全局单例键值存储                                    │  │
│  │  ┌─────────────────────────────────────────────────────────────┐    │  │
│  │  │  unordered_map<string, string>  │  shared_mutex            │    │  │
│  │  │  name → zhangsan                │  读写锁保护             │    │  │
│  │  └─────────────────────────────────────────────────────────────┘    │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ⚠️ V2 将升级：分段锁哈希表 + 过期字典 + LRU                           │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 │ 存储结果
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           网络层 (Network)                                   │
│                                                                             │
│  TCP 通信 ─── 如何接收客户端数据？如何发送响应？                            │
│  ┌─────────────────────────────────────────────────────────────────────┐  │
│  │                                                                   │  │
│  │   EventLoop ─── 事件循环核心，epoll 事件驱动                        │  │
│  │       │                                                          │  │
│  │       ├── Socket ─── TCP 套接字封装                               │  │
│  │       ├── Channel ─── 文件描述符事件封装                          │  │
│  │       ├── Connection ─── 客户端连接管理                             │  │
│  │       └── Buffer ─── TCP 读写缓冲区                               │  │
│  │                                                                   │  │
│  └─────────────────────────────────────────────────────────────────────┘  │
│                                                                             │
│  ⚠️ V2 将升级：MainSubReactor 多线程模型                                │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          基础设施层 (Foundation)                            │
│                                                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐                │
│  │ Logger  │  │ Config  │  │ Signal  │  │ Format  │                │
│  │ V2升级  │  │ V2升级  │  │ V1基础  │  │ V2新增  │                │
│  │ 🔄进行  │  │ 🔄进行  │  │ ✅完成  │  │ ✅完成  │                │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘                │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 1.4.2 每层职责

| 层级 | 职责 | 关键词 | V2 升级 |
|------|------|--------|--------|
| 应用层 | 接收命令，调用存储，返回结果 | 命令分发 | 增加更多命令 |
| 协议层 | 解析字节流，理解命令 | 数据格式 | - |
| 存储层 | 管理键值对，保证安全 | 数据结构 | 分段锁+过期+LRU |
| 网络层 | 收发网络数据，事件驱动 | IO 模型 | MainSubReactor |
| 基础层 | 日志、配置、信号、格式化 | 工程化 | Sink+热加载 |

### 1.4.3 组件依赖关系

```
                    ┌─────────────┐
                    │   Logger    │  ← 基础工具，所有层都用
                    │   Config    │  ← 基础工具，所有层都用
                    └──────┬──────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Network Layer                                       │
│                                                                             │
│      ┌─────────────────────────────────────────────────────────────────┐   │
│      │                         EventLoop                                 │   │
│      │                                                                  │   │
│      │   ┌─────────┐    ┌─────────┐    ┌─────────┐                   │   │
│      │   │ Socket  │───▶│ Channel │───▶│Connection│                  │   │
│      │   └─────────┘    └─────────┘    └────┬────┘                   │   │
│      │                                            │                      │   │
│      │   ┌─────────┐                           │                      │   │
│      │   │ Buffer  │◀──────────────────────────┘                      │   │
│      │   └─────────┘                                                   │   │
│      └─────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Protocol Layer                                      │
│                                                                             │
│      ┌─────────────┐    ┌─────────────┐                                   │
│      │ RespParser  │───▶│ RespEncoder │                                   │
│      └─────────────┘    └─────────────┘                                   │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          Application Layer                                    │
│                                                                             │
│      ┌─────────────────────────────────────────────────────────────────┐   │
│      │                    CommandFactory                                  │   │
│      │    ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │   │
│      │    │   GET   │  │   SET   │  │   DEL   │  │  EXISTS │        │   │
│      │    └─────────┘  └─────────┘  └─────────┘  └─────────┘        │   │
│      └─────────────────────────────────────────────────────────────────┘   │
└────────────────────────────────┬────────────────────────────────────────────┘
                                 ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Storage Layer                                       │
│                                                                             │
│      ┌─────────────────────────────────────────────────────────────────┐   │
│      │                     GlobalStorage                                 │   │
│      │              unordered_map + shared_mutex                         │   │
│      └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 1.5 核心设计理念

### 1.5.1 为什么选择事件驱动而不是多线程？

```
多线程方案的问题：
┌─────────┐ ┌─────────┐ ┌─────────┐
│ 线程1   │ │ 线程2   │ │ 线程3   │   ← 每个线程都要占用栈内存
│ 等待I/O │ │ 等待I/O │ │ 等待I/O │   ← 大部分时间在等，浪费CPU
└─────────┘ └─────────┘ └─────────┘

事件驱动方案：
┌─────────────────────────────────────┐
│           EventLoop (单线程)        │
│                                     │
│   epoll_wait() 阻塞等待 ──► 有事才处理
│                                     │
│   处理 A ──► 处理 B ──► 处理 C      │  ← CPU 始终在工作
└─────────────────────────────────────┘
```

**结论**：

| 场景 | 推荐方案 |
|------|----------|
| IO 密集型（网络服务器） | 事件驱动 ✓ |
| CPU 密集型（计算任务） | 多线程 |
| 混合型 | 事件驱动 + 线程池 |

### 1.5.2 为什么要分层？

```
不分层的问题：
- 全部代码混在一起
- 改网络逻辑要动业务代码
- 难以测试、难以复用
- 扩展新命令很麻烦

分层后的优势：
- 每层只管自己的事
- 可以独立测试
- 可以单独替换
- 易于理解和维护
```

### 1.5.3 为什么要用 RAII 和智能指针？

```
手动管理内存的问题：
- 忘记 delete → 内存泄漏
- 重复 delete → 崩溃

RAII + 智能指针：
- 对象生命周期绑定到作用域
- 作用域结束时自动释放
- 不用担心忘记或重复释放
```

---

## 1.6 请求处理流程（SET name zhangsan）

```
1. 客户端发送 "SET name zhangsan\r\n"
   │
   ▼
2. TCP 数据到达服务器的 socket
   │
   ▼
3. epoll 检测到 socket 可读
   │
   ▼
4. EventLoop 回调 Connection::handle_read()
   │
   ▼
5. Connection 从 socket 读取数据到 Buffer
   │
   ▼
6. RespParser 从 Buffer 解析出命令
   │
   ▼
7. CommandFactory 根据命令名创建具体命令对象
   │
   ▼
8. 命令对象执行：从 GlobalStorage 读写数据
   │
   ▼
9. RespEncoder 将执行结果编码成响应
   │
   ▼
10. Connection 将响应写入 Buffer
    │
    ▼
11. epoll 检测到 socket 可写
    │
    ▼
12. EventLoop 回调 Connection::handle_write()
    │
    ▼
13. Connection 将响应发送给客户端
```

---

## 1.7 代码结构

### 1.7.1 目录结构（当前状态）

```
concurrentcache/
├── src/
│   ├── base/
│   │   ├── log.h/log.cpp        ✅ V1完成 / 🔄 V2 Sink+模块+热加载
│   │   ├── format.h/format.cpp  ✅ V2新增：统一格式化
│   │   ├── config.h/config.cpp  🔄 V2进行中：观察者+热加载
│   │   └── signal.h/signal.cpp ✅ V1完成
│   │
│   ├── network/
│   │   ├── socket.h
│   │   ├── buffer.h
│   │   ├── channel.h
│   │   ├── event_loop.h/cpp
│   │   └── connection.h/cpp
│   │
│   ├── protocol/
│   │   └── resp.h/cpp
│   │
│   ├── command/
│   │   ├── command.h
│   │   └── string_cmd.h/cpp
│   │
│   └── cache/
│       └── storage.h/cpp
│
└── docs/
    └── developing/
        ├── Dev.md              ← 开发手册（实时进度）
        ├── Architecture.md     ← 架构文档
        ├── Roadmap.md         ← 路线图
        └── Version1_Detailed/ ← V1 详细文档
```

---

## 1.8 版本能力对比

### V1 已实现功能

| 功能 | 状态 | 说明 |
|------|------|------|
| TCP 监听 | ✅ | 支持端口监听、接受连接 |
| 非阻塞 IO | ✅ | SOCK_NONBLOCK 模式 |
| epoll 事件驱动 | ✅ | 单 Reactor 模型 |
| RESP 协议解析 | ✅ | 支持 5 种数据类型 |
| 命令系统 | ✅ | GET/SET/DEL/EXISTS |
| 线程安全存储 | ✅ | shared_mutex 读写锁 |
| 基础日志 | ✅ | INFO/DEBUG/ERROR 级别 |
| 基础配置 | ✅ | key=value 解析 |

### V1 存在的不足（V2 将解决）

| 不足 | 影响 | V2 解决方案 |
|------|------|-------------|
| 单线程 Reactor | 无法利用多核 CPU | MainSubReactor |
| 全局一把锁 | 高并发时锁竞争严重 | 分段锁哈希表 |
| 无内存池 | malloc/free 开销大 | 三层内存池 |
| 无过期机制 | 内存无限增长 | 过期字典 |
| 无 LRU | 内存用满无法写 | LRU 算法 |
| 命令有限 | 只有 4 个命令 | 完整字符串命令 |
| 日志无文件 | 重启后日志消失 | FileSink + 热加载 |
| 配置无热加载 | 改配置要重启 | ConfigObserver |

---

## 1.9 版本历史

| 版本 | 日期 | 架构特点 |
|------|------|---------|
| V1.0 | 2026-04-24 之前 | 骨架版本：单Reactor + 简单存储 |
| V2.0 | 🔄 进行中 | 基础版本：内存池 + 线程池 + 缓存核心 |
| V3.0 | 📋 计划中 | 增强版本：多数据类型 + RDB/AOF |
| V4.0 | 📋 计划中 | 分布式版本：集群支持 |

---

## 1.10 下一步

V1 骨架版本完成后，进入 **V2 基础版本**：

详见 `../Roadmap.md` 或 `../Dev.md`

下一章我们将深入**应用层**，了解命令系统的设计：
- 命令模式是什么？如何封装命令？
- 命令工厂如何创建命令？
- 如何添加新命令？

[下一章：应用层](./02_应用层.md)
